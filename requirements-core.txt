# CPU-capable EM / patch RL / evaluation runtime (Python 3.11 or 3.12).
# Install the CPU-only torch wheel separately when CUDA is not needed.
numpy>=1.24,<2
scipy>=1.10,<1.17
pandas>=2,<2.4
torch>=2.2,<3
h5py>=3.9,<4
PyYAML>=6,<7
pyarrow>=14
scikit-learn>=1.3,<2
scikit-image>=0.24,<0.26
matplotlib>=3.7
Pillow>=10.4
tqdm>=4.65
